# Kuna Decompilations: Profile Sort & Structure Rendering Pipeline

This document records the decompilations generated by `kuna` for all 7 previously unnamed (`FUN_`) functions identified in [profile_sort_corruption_analysis.md](profile_sort_corruption_analysis.md). Target binary: **Halo Xbox debug build 2276** (`cachebeta.xbe`, version `01.10.12.2276`, Oct 12, 2001).

---

## 1. `FUN_00024950` (`0x24950`) — AI Firing Position Priority Predicate

* **Source File**: `c:\halo\SOURCE\ai\actor_firing_position.c` (assert at line `0x50f`)
* **Calling Convention**: cdecl, stack args (`index1`, `index2`). Returns `bool` in `AL`.
* **Assert Evidence**:
  - `0x254d88`: `"global_temporary_sort_firing_position_array"`
  - `0x254c8c`: `"c:\\halo\\SOURCE\\ai\\actor_firing_position.c"`
  - `0x254d40`: `"(index1 >= 0) && (index1 < global_temporary_sort_firing_position_count)"`
  - `0x254cf8`: `"(index2 >= 0) && (index2 < global_temporary_sort_firing_position_count)"`

```c
uint4 sub_24950(int4 a0, int4 a1)
{
  float4 v1, v2;
  int4 v3;
  uint4 v4;
  int4 v5, v6;
  uint2 v7;
  
  v3 = a0;
  v5 = a0 * 0x3c + dat_331f04; // global_temporary_sort_firing_position_array
  v6 = a1 * 0x3c + dat_331f04;

  if (!dat_331f04) {
    display_assert("global_temporary_sort_firing_position_array",
                   "c:\\halo\\SOURCE\\ai\\actor_firing_position.c", 0x50f, 1);
    system_exit(-1);
  }
  if ((v3 < 0) || (dat_331f00 <= v3)) {
    display_assert("(index1 >= 0) && (index1 < global_temporary_sort_firing_position_count)",
                   "c:\\halo\\SOURCE\\ai\\actor_firing_position.c", 0x510, 1);
    system_exit(-1);
  }
  if ((a1 < 0) || (dat_331f00 <= a1)) {
    display_assert("(index2 >= 0) && (index2 < global_temporary_sort_firing_position_count)",
                   "c:\\halo\\SOURCE\\ai\\actor_firing_position.c", 0x511, 1);
    a1 = system_exit(-1);
  }

  // Primary sort key: byte at +0x30
  if (*(char *)(v5 + 0x30) != *(char *)(v6 + 0x30))
    return (uint4)(0 < (int4)((uint4)(*(char *)(v5 + 0x30) == '\0') * 2 + -1));

  // Secondary sort key: byte at +0x31
  if (*(char *)(v5 + 0x31) != *(char *)(v6 + 0x31))
    return (uint4)(0 < (int4)((uint4)(*(char *)(v5 + 0x31) != '\0') * 2 + -1));

  // Tertiary sort key: float at +0x38 (score/distance)
  v1 = *(float4 *)(v5 + 0x38);
  v2 = *(float4 *)(v6 + 0x38);
  v7 = (uint2)(v1 < v2) << 8 | (uint2)(NAN(v1) || NAN(v2)) << 10 | (uint2)(v1 == v2) << 0xe;
  dat_2 = (unsigned short)((uint4)a1 >> 0x10);
  v4 = CONCAT22(dat_2, v7);

  if (v7 & 0x4100) {
    v1 = *(float4 *)(v5 + 0x38);
    v2 = *(float4 *)(v6 + 0x38);
    v4 = CONCAT22(dat_2, (uint2)(v1 < v2) << 8 | (uint2)(NAN(v1) || NAN(v2)) << 10 | (uint2)(v1 == v2) << 0xe);
    if (v1 < v2)
      return CONCAT31((undefined3)(v4 >> 8), 1);
  }
  return v4;
}
```

---

## 2. `FUN_00091cf0` (`0x91cf0`) — Generic 16-Bit Selection Sort

* **Source File**: `cseries\profile.c`
* **Calling Convention**: `end` passed in `EAX`, `begin` at `[ebp+8]`, `compare` at `[ebp+0xc]`.
* **Call Test**: `TEST AL, AL` (`v2 = (*a1)(*v6, *v4); if (v2)`).

```c
void sub_91cf0(unsigned short *a0, code *a1)
{
  unsigned short v1;
  char v2; // AL
  unsigned short *v3; // EAX (end pointer)
  unsigned short *v4, *v5, *v6;
  
  v4 = a0;
  if (v3 <= a0)
    return;

  do {
    v6 = &v4[1];
    v5 = v4;
    if (v6 <= v3) {
      do {
        v2 = (*a1)(*v6, *v4);
        if (v2)
          v4 = v6;
        v6 = &v6[1];
      } while (v6 <= v3);
      v5 = a0;
    }
    v1 = *v4;
    *v4 = *v3;
    *v3 = v1;
    v3 = &v3[-1];
    v4 = v5;
  } while (v5 < v3);
}
```

---

## 3. `FUN_00091d50` (`0x91d50`) — Generic 32-Bit Selection Sort

* **Source File**: `cseries\profile.c`
* **Calling Convention**: `end` passed in `EAX`, `begin` at `[ebp+8]`, `compare` at `[ebp+0xc]`.
* **Call Test**: `TEST AL, AL` (`v2 = (*a1)(*v6, *v4); if (v2)`).

```c
void sub_91d50(unsigned int *a0, code *a1)
{
  unsigned int v1;
  char v2; // AL
  unsigned int *v3; // EAX (end pointer)
  unsigned int *v4, *v5, *v6;
  
  v4 = a0;
  if (v3 <= a0)
    return;

  do {
    v6 = &v4[1];
    v5 = v4;
    if (v6 <= v3) {
      do {
        v2 = (*a1)(*v6, *v4);
        if (v2)
          v4 = v6;
        v6 = &v6[1];
      } while (v6 <= v3);
      v5 = a0;
    }
    v1 = *v4;
    *v4 = *v3;
    *v3 = v1;
    v3 = &v3[-1];
    v4 = v5;
  } while (v5 < v3);
}
```

---

## 4. `FUN_00091ef0` (`0x91ef0`) — Generic Hybrid Quicksort / Selection Sort

* **Source File**: `cseries\profile.c`
* **Calling Convention**: cdecl (`keys` at `[ebp+8]`, `count` at `[ebp+0xc]`, `cmp` at `[ebp+0x10]`).
* **Threshold**:
  - `count <= 8`: delegates directly to `sub_91d50` (selection sort).
  - `count >= 9`: midpoint-pivot partition, iterative stack using buffers `v6[30]` and `v7[30]`.
* **Call Test**: `TEST AL, AL` at `0x91f86` and `0x91fa3`.

```c
void sub_91ef0(unsigned int *a0, uint4 a1, code *a2)
{
  unsigned int v1;
  unsigned int *v10;
  unsigned int *v11;
  int4 v2;
  char v3; // AL
  uint4 v4;
  unsigned int *v5;
  unsigned int v6[30];
  unsigned int v7[30];
  unsigned int *v8, *v9;
  
  if (a1 <= 1)
    return;

  v2 = a1 - 1;
  a1 = 0;
  v11 = &a0[v2];
  v8 = a0;

label_91f20:
  v4 = ((int4)v11 - (int4)v8 >> 2) + 1;
  if (9 <= v4) {
    v4 >>= 1;
    v1 = v8[v4];
    v8[v4] = *v8;
    *v8 = v1;
    v9 = &v11[1];
    v5 = v8;

label_91f70:
    v5 = &v5[1];
    if (v5 <= v11) goto label_91f7a;
    goto label_91f90;
  }

  sub_91d50(v8, a2);
  goto label_91f3f;

label_91f7a:
  v3 = (*a2)(*v5, *v8);
  if (!v3) goto label_91f70;

label_91f90:
  do {
    v10 = v9;
    v9 = &v10[-1];
    if (v9 <= v8) break;
    v3 = (*a2)(*v9, *v8);
  } while (v3);

  if (v5 <= v9) {
    v1 = *v5;
    *v5 = *v9;
    *v9 = v1;
    goto label_91f70;
  }

  v1 = *v8;
  *v8 = *v9;
  *v9 = v1;

  if ((int4)v11 - (int4)v5 <= (int4)v9 + (0xffffffffU - (int4)v8)) {
    if (&v8[1] < v9) {
      v7[a1] = (unsigned int)v8;
      v6[a1] = (unsigned int)&v10[-2];
      a1 += 1;
    }
    if (v5 < v11) {
      v8 = v5;
      goto label_91f20;
    }
  } else {
    if (v5 < v11) {
      v7[a1] = (unsigned int)v5;
      v6[a1] = (unsigned int)v11;
      a1 += 1;
    }
    if (&v8[1] < v9) {
      v11 = &v10[-2];
      goto label_91f20;
    }
  }

label_91f3f:
  a1 -= 1;
  if ((int4)a1 <= -1)
    return;
  v11 = (unsigned int *)v6[a1];
  v8 = (unsigned int *)v7[a1];
  goto label_91f20;
}
```

---

## 5. `FUN_00195530` (`0x195530`) — Structure Surface Index Greater-Than Comparator

* **Source File**: `structures\structures.c`
* **Calling Convention**: cdecl (`param_1` at `[ebp+8]`, `param_2` at `[ebp+0xc]`).
* **Behavior**: Evaluates `param_2 < param_1`. Notice `CONCAT31(v1, a1 < a0)` explicitly models that bits 8..31 of `EAX` (`v1`) remain unchanged from the caller.

```c
int4 sub_195530(int4 a0, int4 a1)
{
  undefined3 v1; // EAX upper 24 bits untouched
  
  v1 = (undefined3)((uint4)a0 >> 8);
  if (a0 < a1)
    return (uint4)v1 << 8;
  return CONCAT31(v1, a1 < a0);
}
```

---

## 6. `FUN_00195650` (`0x195650`) — Sorted Structure Surface Tag Populator

* **Source File**: `structures\structures.c`
* **Calling Convention**: Register args: `out` in `EAX`, `indices` in `ECX`, and `count` in `EDX`.
* **Behavior**: Calls `sub_91ef0` to sort surface indices ascending, then loops copying 3 `uint16_t` values (`short[3]`, stride 6) from `scenario_bsp->surfaces` (`scenario + 0xf8`).

```c
void sub_195650(unsigned short *out /* @<eax> */,
                unsigned int *indices /* @<ecx> */,
                uint2 count /* @<edx> */)
{
  unsigned short *v1 = out; // EAX (destination triangle buffer)
  int4 v2; // scenario pointer
  unsigned short *v3;
  uint4 v4;
  
  v2 = scenario_get(); // sub_18e3c0
  sub_91ef0(indices, (int4)(int2)count, 0x195530);

  if ((int2)count <= 0)
    return;

  v4 = (uint4)count;
  do {
    v3 = (unsigned short *)tag_block_get_element(v2 + 0xf8, *indices, 6); // sub_19b210
    *v1 = *v3;
    v1[1] = v3[1];
    v1[2] = v3[2];
    indices = &indices[1];
    v1 = &v1[3];
    v4 -= 1;
  } while (v4);
}
```

---

## 7. `FUN_001956d0` (`0x1956d0`) — Dynamic Structure Triangle Widget Submitter

* **Source File**: `c:\halo\SOURCE\structures\structure_render.c` (assert at line `0x1e6` / 486)
* **Calling Convention**: `count` in `ESI` (`v2`), `param_1` on stack, `param_2` on stack (`a1`).
* **Assert Evidence**:
  - `0x2b34e8`: `"unable to allocate dynamic structure triangles."`
  - `0x2a2c84`: `"triangles"`
  - `0x2b347c`: `"c:\\halo\\SOURCE\\structures\\structure_render.c"`
* **Behavior**:
  1. Calls `rasterizer_widget_submit` (`sub_17c970`).
  2. Maps vertex/triangle buffer via `rasterizer_widget_begin` (`sub_17c980`).
  3. If `mask == NULL`: calls `sub_195650` to sort and copy surface indices.
  4. If `mask != NULL`: calls `sub_195550` to gather mask-selected surfaces.
  5. Submits widget texture via `rasterizer_widget_set_texture` (`sub_17c990`).

```c
int4 sub_1956d0(unsigned int a0, int4 a1)
{
  int4 v1; // widget slot handle
  int2 v2; // SI (surface count)
  
  scenario_get();
  if (v2 <= 0)
    return -1;

  v1 = rasterizer_widget_submit((int4)v2); // sub_17c970
  if (v1 == -1) {
    if (!dat_32bd60)
      return -1;
    display_warning(2, "unable to allocate dynamic structure triangles."); // 0x2b34e8
    dat_32bd60 = 0;
    return -1;
  }

  if (!rasterizer_widget_begin(v1)) { // sub_17c980
    display_assert("triangles", "c:\\halo\\SOURCE\\structures\\structure_render.c", 0x1e6, 1);
    system_exit(-1);
  }

  if (!a1) {
    sub_195650(); // sort and copy surface triangles
    rasterizer_widget_set_texture(v1); // sub_17c990
    return v1;
  }

  sub_195550(); // gather mask-selected surfaces
  rasterizer_widget_set_texture(v1); // sub_17c990
  return v1;
}
```
